Topic Modeling: is an example of unsupervised learning.

- Topic modeling helps us uncover the main themes that run through a collection of documents.

- LDA: Latent Dirichlet Allocation
1. prepare the text
2. build the structures that LDA expects.
3. train the model.

- Gensim is a python library designed for text analysis.


- dictionary = corpora.Dictionary(articles) :
each word in the articles gets its own id which will later allow 
the LDA Model to work with the text in a structured way.


doc_term = [dictionary.doc2bow(text) for text in articles]:
doc2bow(text): takes a single article, looks up each word in the dictionary,
and returns a list of word IDs together with their counts.



- LSA: Latent Semantic Analysis
1. Distributional Hypothesis: states that words with similar meanings appear frequently together.

2.SVD: Singular Value Decomposition: is a method of dimensionality reduction
similarities can be identified by:
- clustering
- similarity scores



